[project]
name = "historicam-training"
version = "0.1.0"
description = "ML model training pipeline for HistoriCam"
requires-python = ">=3.11"
dependencies = [
    "google-cloud-storage>=2.10.0",
    "google-cloud-aiplatform>=1.25.0",
    "tensorflow>=2.13.0",
]
